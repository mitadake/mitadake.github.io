# Local Ollama Cloud key for the chat proxy (not used by the static site directly).
# Copy to proxy/.dev.vars for `wrangler dev`, or set on the deployed Worker:
#   cd proxy && npx wrangler secret put OLLAMA_API_KEY
OLLAMA_API_KEY=your_ollama_api_key_here
